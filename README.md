# Orna Field Guide

Static GitHub Pages site for fan-made Orna and Hero of Aethric guides.

## Local Preview

Open `index.html` directly in a browser, or run:

```sh
python3 -m http.server 8080
```

Then visit `http://localhost:8080`.
